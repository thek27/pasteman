{
  "name": "pasteman",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "debug": "electron . --debug",
    "package:mac": "electron-packager . --overwrite --out=dist --platform=darwin --arch=x64 --icon=assets/app-icon/mac/app.icns --extend-info=assets/mac/info.plist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^9.2.0",
    "electron-packager": "^15.0.0"
  },
  "dependencies": {
    "electron-reload": "^1.5.0",
    "ncp": "^2.0.0",
    "node-fetch": "^2.6.0"
  }
}
